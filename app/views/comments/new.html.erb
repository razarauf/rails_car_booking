<h2> Make a Comment</h2>
<p> Yes you can break it if you don't put in a valid car. Please don't do that </p>
<%= form_for(:comment, :url => comments_path) do |f| %>
  <p>
    <%= f.label :user_id %><br>
   <%= f.hidden_field :user_id, :value => current_user.id %>
  </p> 
  
  <p>
    <%= f.label :car_id %><br>
    <%= f.text_field :car_id %>
  </p>
  
  <p>
    <%= f.label :body %><br>
    <%= f.text_area :body %>
  </p>
 
  <p>
    <%= f.submit %>
  </p>
<% end %>